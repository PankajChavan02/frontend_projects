var msg;
msg ="<p><code>The script is located in external script file called math.js</code></p>";

function addNumbers(head,body){
    var total = head + body
    return "Addition of "+head+" and "+body+" is : "+total
}